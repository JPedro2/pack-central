kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: octane-claim
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.global.storage }}
